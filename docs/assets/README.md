# Assets

Save the product screenshot used by the root README as:

```text
docs/assets/sponsorscope-dashboard.png
```

The README references that path directly so GitHub can render the wide dashboard image near the top of the repository page.
