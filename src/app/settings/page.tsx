export default function SettingsPage() {
  return (
    <div className="max-w-3xl rounded-lg bg-card/60 p-5 ring-1 ring-border/70">
      <h1 className="text-2xl font-semibold tracking-tight">Settings</h1>
      <p className="mt-2 text-muted-foreground">Environment and integration status will live here as the workspace grows.</p>
    </div>
  );
}
